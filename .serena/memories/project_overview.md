# Project Overview
`grocy-receipt-scanner-fe` is the frontend application for scanning receipts for Grocy.
It is an Angular application that uses Angular Material for its UI.
The project runs in a Node.js environment.
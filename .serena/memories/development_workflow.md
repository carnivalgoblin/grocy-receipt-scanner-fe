# Development Workflow

## Install
`npm install`

## Run
`npm start` or `ng serve`
Runs at http://localhost:4200/

## Build
`npm run build` or `ng build`
Artifacts in `dist/`

## Test
`npm test` or `ng test` (Runs Vitest)